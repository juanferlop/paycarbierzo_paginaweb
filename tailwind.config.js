/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // ← importante para que responda a html.dark
  content: ['./**/*.html', './**/*.js'],
  theme: { extend: {} },
  plugins: [],
};
